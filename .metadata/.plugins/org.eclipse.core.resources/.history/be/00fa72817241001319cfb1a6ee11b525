object prgAdvanced {
	type Student = String;
  type Students = List[Students];
  type Hate = List[(Student, String)];
  
  def bar(sits: Students, stands: Students, hate: Hate): List[Students] = {
  	def canSit(s1: Student, s2: Student, hate:Hate):Boolean = !(hate.contain((s1,s2)) && hate.contains((s2,s1)))
  }
}